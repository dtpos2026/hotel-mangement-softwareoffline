{
  "name": "hotel-management-offline",
  "version": "1.0.0",
  "description": "Offline hotel / guest house / resort management system. No dependencies, no build step — index.html runs the app directly.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node tests/run.js && node tests/print.test.js && node tests/integration.test.js"
  }
}
